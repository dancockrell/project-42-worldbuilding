card: iwasaki-chiyo
editor: lm4
verdict: REVISE

- Missing the `image:` field required by WRITING.md's source format block — only `image_caption:` is present. The picture is one of the three things a card is ("Every entry is three things: a picture, a hundred words, and a story of five hundred"); add the `image:` line before this is a complete card.
